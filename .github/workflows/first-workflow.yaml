name: First Workflow 

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a String 
        run: echo "Hello Github Action !!"
      
      - name: Multiline command 
        run: |
          node -v 
          npm -v 
          cat /etc/issue.net
          